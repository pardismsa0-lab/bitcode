import type { Block } from "./content-types";

/* لایه «عمیق‌تر» — بخش ۳: جاوااسکریپت، React، Node */
export const DEEP_C: Record<string, Block[]> = {
  "c8-l1": [
    { k: "h", t: "عمیق‌تر: Scope و زنجیره‌ای که همه‌چیز را توضیح می‌دهد" },
    { k: "p", t: "وقتی جاوااسکریپت متغیری را می‌خواند، از Scope فعلی شروع می‌کند و پله‌پله بالا می‌رود تا global — این «زنجیره Scope» است و تمام رفتارهای عجیب var/let را توضیح می‌دهد. var به نزدیک‌ترین «تابع» می‌چسبد (Function-scoped) و حتی Hoisting می‌شود: قبل از تعریف هم وجود دارد با مقدار undefined — تله‌ای که کدهای قدیمی را پر از باگ کرده. let و const به نزدیک‌ترین «بلوک» می‌چسبند و تا خط تعریف، در «منطقه مرده موقت» (TDZ) هستند؛ دسترسی زودتر از موعد، خطا می‌دهد — همان چیزی که می‌خواستیم. قاعده مدرن: همیشه const، فقط وقتی واقعاً reassing می‌شود let، و var فقط وقتی کد ۲۰۱۰ را می‌خوانی." },
    { k: "p", t: "DOM را نه به‌عنوان «HTML در جاوااسکریپت»، که به‌عنوان یک API درختی ببین: هر تگ یک شیء است و تغییرش هزینه دارد. بزرگ‌ترین اشتباه تازه‌کارها، خواندن/نوشتن DOM در حلقه است — هر بار Reflow احتمالی. الگوی حرفه‌ای: داده را در حافظه تغییر بده، یک‌بار به DOM بنویس (یا از DocumentFragment برای درج گروهی استفاده کن). و رویدادها را با «تفویض» (Delegation) مدیریت کن: یک listener روی والد، به‌جای صد listener روی فرزندان — چون رویدادها از هدف به سمت ریشه حباب می‌شوند (Bubbling)، والد می‌تواند با e.target بفهمد کدام فرزند کلیک شده. هم حافظه کمتر، هم برای عناصری که بعداً اضافه می‌شوند کار می‌کند." },
    { k: "code", lang: "js", title: "deepdive-delegation.js", code: "// ضدالگو: ۱۰۰ listener\nitems.forEach(li => li.addEventListener('click', handler));\n\n// الگوی تفویض: یک listener، امروز و برای آیتم‌های آینده\ndocument.querySelector('#list').addEventListener('click', (e) => {\n  const li = e.target.closest('li');   // نزدیک‌ترین جدِ li\n  if (!li) return;                     // کلیک جای دیگر بوده\n  console.log('انتخاب شد:', li.dataset.id);\n});\n\n// درج گروهی بدون Reflow مکرر:\nconst frag = document.createDocumentFragment();\nfor (let i = 0; i < 1000; i++) {\n  const li = document.createElement('li');\n  li.textContent = `آیتم ${i}`;\n  frag.appendChild(li);\n}\nlist.appendChild(frag);   // فقط یک بار به DOM واقعی\n\n// TDZ — چرا let بهتر از var است:\n// console.log(x);  → ReferenceError (خوب! باگ را زود نشان داد)\nconst x = 10;\n// اگر var بود: undefined می‌دیدیم و باگ قایم می‌ماند" },
    { k: "table", head: ["ویژگی", "var", "let", "const"], rows: [["محدوده", "تابع", "بلوک", "بلوک"], ["Hoisting با مقدار", "بله (undefined) — تله", "TDZ — خطا می‌دهد", "TDZ — خطا می‌دهد"], ["تغییرپذیری", "بله", "بله", "خیر (برای مرجع اشیاء)"], ["توصیه", "هرگز", "وقتی reassing لازم است", "پیش‌فرض همیشگی"]] },
    { k: "tip", title: "نکته منتور", t: "برای درک Bubbling، یک DevTools باز کن و در تب Elements، روی یک دکمه عمیق listener بگذار و ببین رویداد از کجاها رد می‌شود. درک این حباب، نیمی از سوال‌های مصاحبه DOM (stopPropagation، preventDefault، تفاوت capture/bubble) را یک‌جا حل می‌کند." },
  ],

  "c8-l2": [
    { k: "h", t: "عمیق‌تر: Closure — همان ایده‌ای که React را ممکن کرد" },
    { k: "p", t: "Closure یعنی تابع، محیطی که در آن ساخته شده را «به همراه می‌برد»: متغیرهای Scope بیرونی‌اش زنده می‌مانند حتی بعد از اینکه تابع بیرونی برگشته. makeCounter() را که دیدی، Closure را دیدی: شمارنده در متغیر محلی makeCounter زندگی می‌کند و تابع برگشتی، کلیدش را دارد — نه از بیرون قابل‌دسترس، نه از بین می‌رود. این همان «کپسوله‌سازی به سبک جاوااسکریپت» است قبل از آنکه class وجود داشته باشد. و مهم‌تر: Hooks در React (useState و همه دوستانش) دقیقاً روی همین Closureها سوارند — وقتی می‌فهمی چرا state در کامپوننت ماندگار است، یعنی Closure را فهمیده‌ای." },
    { k: "p", t: "چهار قاعده this را مثل جدول ضرب حفظ کن، چون نصف باگ‌های «کلاسیک» جاوااسکریپت از همین‌جاست: ۱) فراخوانی ساده obj.fn() → this می‌شود obj؛ ۲) فراخوانی تنها fn() → در strict می‌شود undefined؛ ۳) Arrow Function این خودش را «ندارد» و از محیط ارث می‌برد — پس در callbackهای داخل متد کلاس، Arrow تنها راه درست است؛ ۴) bind/call/apply می‌توانند دستی this را ببندند. اگر روزی دیدی this ناگهان undefined شده، یعنی تابع از مالکش «جدا» شده (مثلاً پاس‌داده‌شده به عنوان callback) — و درمانش Arrow یا bind است." },
    { k: "code", lang: "js", title: "deepdive-closure.js", code: "function makeCounter(start = 0) {\n  let count = start;              // متغیر «زندانی» Closure\n  return {\n    inc:  () => ++count,\n    get:  () => count,\n  };\n}\nconst c = makeCounter(10);\nc.inc(); c.inc();\nconsole.log(c.get());   // 12 — و هیچ راهی برای دستکاری مستقیم count نیست\n\n// تله کلاسیک مصاحبه:\nfor (var i = 0; i < 3; i++) setTimeout(() => console.log(i), 0);\n// خروجی: 3 3 3  ← var یک متغیر واحد است؛ همه Closureها همان را می‌بینند\n\nfor (let i = 0; i < 3; i++) setTimeout(() => console.log(i), 0);\n// خروجی: 0 1 2  ← let در هر دور تکرار، متغیر تازه می‌سازد\n\n// Arrow و this — چرا داخل متد کلاس Arrow می‌نویسیم:\nclass Timer {\n  seconds = 0;\n  start() {\n    setInterval(() => {\n      this.seconds += 1;          // this از محیط (start) ارث برده — درست\n    }, 1000);\n    // اگر تابع معمولی بود: this = undefined → TypeError\n  }\n}" },
    { k: "p", t: "توابع مرتبه‌بالا — توابعی که تابع می‌گیرند یا برمی‌گردانند — ابزار اصلی «انتزاع رفتار» در جاوااسکریپت‌اند: debounce و throttle (کنترل نرخ فراخوانی)، memoize (کش نتایج)، once (اجرای یک‌بار) و curry همه با سه خط Closure ساخته می‌شوند. یادگیری این چهار تابع کوچک، همزمان Closure را تثبیت می‌کند و جعبه‌ابزار روزمره‌ات را واقعی می‌کند — چون دیر یا زود در هر پروژه UI به debounce برای فیلد جست‌وجو نیاز پیدا می‌کنی." },
    { k: "code", lang: "js", title: "deepdive-debounce.js", code: "// تا ۳۰۰ms بعد از آخرین تایپ، اجرا نشود:\nfunction debounce(fn, delay = 300) {\n  let timer;\n  return (...args) => {\n    clearTimeout(timer);                // هر تایپ، تایمر قبلی را باطل می‌کند\n    timer = setTimeout(() => fn(...args), delay);\n  };\n}\n\nsearchInput.addEventListener('input', debounce((e) => {\n  fetch(`/api/search?q=${e.target.value}`);\n}, 300));\n// نتیجه: به‌جای ۲۰ درخواست برای «ساختمان داده»، فقط ۱ درخواست" },
    { k: "tip", title: "نکته منتور", t: "سوال کلاسیک «چرا حلقه var سه تا ۳ چاپ می‌کند؟» در مصاحبه‌های فرانت‌اند هنوز زنده است. جواب کامل سه بخش دارد: var به تابع می‌چسبد نه بلوک، Closureها مرجع را نگه می‌دارند نه مقدار را، و let در هر تکرار Binding تازه می‌سازد. این جواب، درک عمیق زبان را در ۳۰ ثانیه ثابت می‌کند." },
  ],

  "c8-l3": [
    { k: "h", t: "عمیق‌تر: Event Loop، میکروتسک‌ها و چرا ترتیب خروجی گیج می‌کند" },
    { k: "p", t: "جاوااسکریپت تک‌نخی است — فقط یک کار همزمان انجام می‌دهد — و با این حال وب همزمان هزار کار می‌کند. راز: کارهای «کند» (شبکه، تایمر، فایل) را به محیط می‌سپارد و وقتی تمام شدند، callbackشان در صف می‌نشیند؛ Event Loop هم هر وقت پشته خالی شد، یکی را برمی‌دارد. ظرافت کلیدی اینجاست که دو صف وجود دارد: میکروتسک‌ها (Promiseها) اولویت مطلق دارند و تا خالی نشوند، هیچ ماکروتسک‌ای (setTimeout) اجرا نمی‌شود. پس console.log اول، بعد .thenها، بعد timeout — حتی اگر timeout با تأخیر صفر ثبت شده باشد. این ترتیب، جواب تمام سوال‌های «خروجی این کد چیست» مصاحبه است." },
    { k: "p", t: "async/await فقط پوشش خوش‌خط‌وتر برای Promise است: تابع async همیشه Promise برمی‌گرداند و await یعنی «اینجا تابع را متوقف کن، بقیه برنامه ادامه دارد، وقتی Promise نشست برگرد». اما سه تله دارد که حرفه‌ای‌ها را هم می‌گیرد: ۱) await داخل حلقه یعنی درخواست‌های سریالی — وقتی مستقل‌اند باید Promise.all زد و زمان را از جمع به بیشینه رساند؛ ۲) بدون try/catch، خطای Promise وسط تابع گم می‌شود و به unhandled rejection تبدیل می‌شود؛ ۳) await کردن چیزی که Promise نیست اشکالی ندارد ولی بیهوده است. و Promise.allSettled را بشناس: وقتی می‌خواهی همه نتایج (حتی شکست‌ها) را بگیری و یک خطا کل دسته را خراب نکند." },
    { k: "code", lang: "js", title: "deepdive-eventloop.js", code: "// ترتیب خروجی را قبل از اجرا حدس بزن:\nconsole.log('۱ — هماهنگ');\n\nsetTimeout(() => console.log('۴ — timeout (ماکروتسک)'), 0);\n\nPromise.resolve()\n  .then(() => console.log('۳ — promise (میکروتسک)'));\n\nconsole.log('۲ — هماهنگ');\n// ترتیب واقعی: ۱، ۲، ۳، ۴ — حتی با timeout صفر!\n\n// ضدالگو: درخواست‌های سریالی — ۳ ثانیه\nfor (const id of ids) {\n  const user = await fetchUser(id);   // هر بار صبر کن!\n}\n\n// الگوی درست: موازی — ۱ ثانیه\nconst users = await Promise.all(ids.map(fetchUser));\n\n// اگر مستقل نیستند (هر کدام به قبلی نیاز دارد):\nlet cursor = null;\nconst pages = [];\nwhile (cursor !== undefined) {        // زنجیره واقعی وابستگی\n  const page = await fetchPage(cursor);\n  pages.push(page.data);\n  cursor = page.next;\n}" },
    { k: "table", head: ["ابزار", "کی استفاده کنیم"], rows: [["Promise.all", "چند عملیات مستقل — همه باید موفق شوند"], ["Promise.allSettled", "چند عملیات مستقل — نتیجه هرکدام (موفق/شکست) را جدا بخواه"], ["Promise.race", "مسابقه: اولین نتیجه (مثلاً timeout در برابر API)"], ["Promise.any", "اولین موفقیت — شکست‌ها مهم نیستند"], ["for...of + await", "فقط وقتی واقعاً زنجیره وابستگی داری"]] },
    { k: "warn", title: "حلقه بی‌پایان مخفی", t: "یک while که فقط میکروتسک تولید می‌کند (Promiseهای تو در تو)، Event Loop را برای همیشه گروگان می‌گیرد و UI قفل می‌شود — چون صف میکروتسک‌ها هیچ‌وقت خالی نمی‌شود تا نوبت به رندر برسد. کارهای سنگین محاسباتی را هم در Thread اصلی نگذار؛ Web Worker یا chunking با setTimeout راه‌حل‌های استانداردند." },
  ],

  "c9-l1": [
    { k: "h", t: "عمیق‌تر: Reconciliation — چرا React «فکر می‌کند» قبل از اینکه بنویسد" },
    { k: "p", t: "نوشتن مستقیم DOM کند و پر از باگ است چون «وضعیت» را پخش می‌کند: ده جای کد، ده جا درخت را عوض می‌کنند و کسی نمی‌داند صفحه الان واقعاً چه وضعیتی دارد. انقلاب React یک معادله ساده بود: UI = f(state). تو فقط وضعیت را توصیف می‌کنی؛ React نسخه مجازی و سبکی از درخت (Virtual DOM) می‌سازد، با نسخه قبلی مقایسه می‌کند (Diff)، و فقط تفاوت‌ها را به DOM واقعی می‌نویسد. نتیجه: ذهن تو روی «داده» متمرکز می‌ماند و هماهنگی UI، مسئله سیستم می‌شود نه مسئله تو. این «اعلانی» (Declarative) بودن، همان چیزی است که توسعه رابط کاربری را از هنرِ حالت‌داری به مهندسی تبدیل کرد." },
    { k: "p", t: "کلیدها (keys) را هیچ‌وقت جدی نگیر یعنی Diff را کور کنی: React برای تشخیص اینکه «کدام عنصر همان عنصر قبلی است» در لیست‌ها، به key نگاه می‌کند. ایندکس آرایه به‌عنوان key وقتی داده‌ها حذف/جابه‌جا می‌شوند فاجعه می‌سازد — state هر ردیف به ردیف اشتباه می‌چسبد و باگ‌هایی می‌بینی که فقط «گاهی» رخ می‌دهند. key باید هویت پایدار داده باشد (id دیتابیس). و یک‌طرفه‌بودن جریان داده را هم جدی بگیر: داده از والد به فرزند پایین می‌آید (props) و رویدادها بالا می‌روند (callback) — به محض اینکه بخواهی این جهت را بشکنی، داری با فریم‌ورک می‌جنگی و حالت Lifting لازم است." },
    { k: "code", lang: "tsx", title: "deepdive-keys.tsx", code: "// ❌ ایندکس به‌عنوان key — با حذف آیتم، stateها جابه‌جا می‌شوند\n{todos.map((todo, i) => <TodoRow key={i} todo={todo} />)}\n\n// ✓ هویت پایدار — هر ردیف، خودش می‌ماند\n{todos.map((todo) => <TodoRow key={todo.id} todo={todo} />)}\n\n// جریان یک‌طرفه + بالا بردن state:\nfunction TodoApp() {\n  const [todos, setTodos] = useState<Todo[]>([]);\n\n  const addTodo = (text: string) =>            // رویداد بالا می‌آید\n    setTodos((prev) => [...prev, makeTodo(text)]);\n\n  return (\n    <>\n      <TodoInput onAdd={addTodo} />            {/* داده/رفتار پایین می‌رود */}\n      <TodoList todos={todos} />\n    </>\n  );\n}\n// TodoInput خودش state ندارد — «کنترل‌شده» است؛\n// حقیقت واحد در TodoApp زندگی می‌کند و باگ sync وجود ندارد." },
    { k: "p", t: "JSX هم که اول عجیب به‌نظر می‌رسد، در واقع کامپایل می‌شود به فراخوانی‌های تابع — و این یعنی دو نتیجه مهم: اول، هر جا بتوانی تابع بنویسی می‌توانی UI بسازی (map برای لیست‌ها، && برای شرط‌ها، متغیر برای بخش‌بندی)؛ دوم، کامپوننت فقط یک تابع است و همه ابزارهای جاوااسکریپت — ترکیب، انتزاع، تایپ — رویش کار می‌کنند. به همین دلیل الگوی «کامپوننت کوچک» در React جواب می‌دهد: کامپوننت رایگان است، پس بی‌رحمانه بشکن — هر بلوک بصری که نامی دارد، شایسته کامپوننت خودش است." },
    { k: "tip", title: "نکته منتور", t: "قبل از نوشتن هر صفحه در React، سه دقیقه روی کاغذ انجام بده: ۱) مرز کامپوننت‌ها را دور بخش‌های بصری بکش، ۲) داده هر بخش را مشخص کن، ۳) state مشترک را به «نزدیک‌ترین جد مشترک» ببر. این سه قدم، همان متدولوژی رسمی «Thinking in React» است و ساختن UI را از آزمون‌وخطا نجات می‌دهد." },
  ],

  "c9-l2": [
    { k: "h", t: "عمیق‌تر: State، Batch و به‌روزرسانی تابعی" },
    { k: "p", t: "setState مثل بقیه توابع، همزمان اجرا نمی‌شود؛ React چند به‌روزرسانی را جمع می‌کند (Batching) و یک‌بار رندر می‌کند — برای سرعت. نتیجه‌اش تله‌ای است که همه می‌افتند: دو بار پشت‌سرهم setCount(count + 1) بنویسی، count فقط یکی زیاد می‌شود، چون هر دو از «همان snapshot قدیمی» خوانده‌اند. درمان، فرم تابعی است: setCount(c => c + 1) — یعنی «هر موقع خواستی اعمال کنی، از آخرین مقدار شروع کن». قانون: هر وقت مقدار جدید از مقدار قبلی محاسبه می‌شود، همیشه فرم تابعی بنویس؛ حتی اگر فعلاً یک‌بار است — چون فردا دومی اضافه می‌شود و باگش فقط شب‌های پرترافیک ظاهر می‌شود." },
    { k: "p", t: "State در React تغییرناپذیر است — نه به‌خاطر سخت‌گیری، به‌خاطر ریاضیات: React برای فهمیدن «چه چیزی تغییر کرد» فقط مرجع‌ها را مقایسه می‌کند (Object.is). اگر آبجکت را مستقیم mutate کنی، مرجع عوض نمی‌شود و React اصلاً نمی‌فهمد اتفاقی افتاده — رندری در کار نیست و ساعت‌ها دیباگ می‌کنی. پس همه به‌روزرسانی‌ها «ساختن نسخه جدید» است: {...obj, field: value} برای آبجکت، [...arr, item] و arr.filter/map برای آرایه. و وقتی state پیچیده شد (چند فیلد با تغییرات وابسته)، useReducer با actionهای نام‌دار، منطق را از کامپوننت بیرون می‌کشد و تست‌پذیرش می‌کند — دقیقاً مثل Redux ولی محلی." },
    { k: "code", lang: "tsx", title: "deepdive-state.tsx", code: "// ❌ تله snapshot قدیمی:\nonClick={() => {\n  setCount(count + 1);\n  setCount(count + 1);    // هنوز همان count قدیمی!\n}}\n// نتیجه: +1 به‌جای +2\n\n// ✓ فرم تابعی:\nonClick={() => {\n  setCount((c) => c + 1);\n  setCount((c) => c + 1);  // از نتیجه قبلی شروع می‌کند → +2\n}}\n\n// ✓ تغییرناپذیری — نسخه جدید بساز، قدیمی را دست نزن:\nconst addItem = (item: Item) =>\n  setCart((prev) => [...prev, item]);\n\nconst updateQty = (id: number, qty: number) =>\n  setCart((prev) =>\n    prev.map((it) => (it.id === id ? { ...it, qty } : it)));\n\n// useReducer برای state چندبخشی:\nconst [form, dispatch] = useReducer(formReducer, initialForm);\ndispatch({ type: 'SET_FIELD', field: 'email', value });\ndispatch({ type: 'RESET' });\n// منطق تغییرات در formReducer است — خالص، خوانا، تست‌پذیر" },
    { k: "table", head: ["وضعیت", "ابزار درست"], rows: [["یک مقدار مستقل", "useState"], ["چند مقدار که با هم تغییر می‌کنند", "یک useState با آبجکت یا useReducer"], ["منطق تغییر پیچیده/چند action", "useReducer (تست‌پذیر و قابل‌ردیابی)"], ["داده از سرور", "React Query — اصلاً state محلی نیست!"], ["فرم بزرگ", "React Hook Form — state را از رندر خارج می‌کند"]] },
    { k: "warn", title: "State اشتباه", t: "قبل از ساختن هر useState بپرس: آیا این واقعاً state است؟ اگر از props قابل‌محاسبه است، اگر از داده سرور قابل‌استخراج است، یا اگر می‌شود هنگام رویداد محاسبه‌اش کرد — state نساز. «State مشتقی» بزرگ‌ترین منبع باگ‌های sync در React است: دو منبع حقیقت که بالاخره یک روز با هم دعوا می‌کنند." },
  ],

  "c9-l3": [
    { k: "h", t: "عمیق‌تر: useEffect بدون اشک — سه قانون و یک مدل ذهنی" },
    { k: "p", t: "useEffect را نه به‌عنوان «جایی که کد اجرا می‌شود»، که به‌عنوان «همگام‌سازی React با دنیای بیرون» ببین: subscribe به WebSocket، عنوان سند، تایمر، درخواست شبکه — چیزهایی که خارج از مدل UI اتفاق می‌افتند. با این مدل ذهنی، سه قانون خودش را توجیه می‌کند: ۱) هر effect مسئول یک همگام‌سازی است؛ ۲) آرایه وابستگی یعنی «وقتی این‌ها عوض شدند، دوباره همگام کن»؛ ۳) اگر effect منبعی باز کرد (اتصال، تایمر، listener)، باید تابع cleanup برگرداند که آن را ببندد — چون قبل از اجرای دوباره و هنگام unmount، React همان cleanup را صدا می‌زند. Memory Leak در React تقریباً همیشه یعنی cleanup فراموش‌شده." },
    { k: "p", t: "آرایه وابستگی قرارداد صداقت است: هر متغیری از scope کامپوننت که effect می‌خواند، باید در آرایه باشد — حذفش با کامنت eslint-disable یعنی ساختن باگی که امروز کار می‌کند و سه ماه بعد با داده‌ای متفاوت منفجر می‌شود. وابستگی خالی [] یعنی «فقط هنگام mount» — و اگر state‌ای را می‌خواند ولی در آرایه نیست، همیشه مقدار اولیه‌اش را می‌بیند (تله کلاسیک closure قدیمی). و حقیقتی که دیر یا زود می‌فهمی: بسیاری از useEffectها اصلاً لازم نیستند — محاسبات مشتقی را وسط رندر انجام بده و اثرات جانبی را به Event Handlerها ببر؛ «شاید useEffect لازم باشد» خودش اولین نشانه است که شاید لازم نباشد." },
    { k: "code", lang: "tsx", title: "deepdive-effect.tsx", code: "// ✓ الگوی کامل: همگام‌سازی + cleanup + وابستگی درست\nfunction useRoomStatus(roomId: string) {\n  const [online, setOnline] = useState(0);\n\n  useEffect(() => {\n    const ws = new WebSocket(`/ws/rooms/${roomId}`);\n    ws.onmessage = (msg) => setOnline(JSON.parse(msg.data).count);\n\n    return () => ws.close();   // cleanup: بستن اتصال قدیمی\n    // اگر نباشد: هر تغییر roomId یک socket زامبی می‌سازد!\n  }, [roomId]);                // وابستگی: roomId استفاده شده ✓\n\n  return online;\n}\n\n// ❌ تله closure قدیمی:\nuseEffect(() => {\n  const id = setInterval(() => setCount(count + 1), 1000);\n  return () => clearInterval(id);\n}, []);                        // count حذف شده → همیشه 0+1!\n\n// ✓ درمان — یا وابستگی، یا فرم تابعی:\nuseEffect(() => {\n  const id = setInterval(() => setCount((c) => c + 1), 1000);\n  return () => clearInterval(id);\n}, []);                        // حالا درست: از آخرین مقدار می‌خواند" },
    { k: "p", t: "وقتی یک effect را در دو کامپوننت کپی کردی، زمان ساخت Custom Hook است — همان تابع‌های معمولی که با use شروع می‌شوند و می‌توانند Hook صدا بزنند. useDebounce، useLocalStorage، useFetch، useOnlineStatus — این‌ها بلوک‌های سازنده واقعی React مدرن‌اند: منطقِ حالت‌دارِ تست‌شده که یک‌بار نوشته و همه‌جا مصرف می‌شود، بدون اینکه کامپوننت‌ها به هم گره بخورند. کتابخانه‌های بزرگ مثل TanStack Query در واقع چیزی جز مجموعه‌ای از Custom Hookهای بسیار مهندسی‌شده نیستند." },
    { k: "tip", title: "نکته منتور", t: "React.StrictMode در توسعه، هر effect را عمداً دو بار اجرا می‌کند (mount → unmount → mount) تا effectهای بدون cleanup را لو بدهد. اگر چیزی در StrictMode دو بار اتفاق می‌افتد، کدت باگ دارد نه React — این بهترین معلم رایگانِ cleanup درست است؛ هرگز خاموشش نکن." },
  ],

  "c10-l1": [
    { k: "h", t: "عمیق‌تر: داخل Event Loop — چرا Node با یک نخ، هزاران اتصال را می‌گیرد" },
    { k: "p", t: "Node همان مدل جاوااسکریپت مرورگر را گرفت و به سرور برد: یک نخ اصلی که فقط کد JS اجرا می‌کند، و کتابخانه libuv که کارهای «بلوکه‌کننده» (خواندن فایل، DNS، شبکه) را به ThreadPool و سیستم‌عامل می‌سپارد. وقتی می‌نویسی fs.readFile، نخ اصلی معطل نمی‌شود — ادامه می‌دهد و وقتی فایل آماده شد، callback در صف می‌نشیند. نتیجه: یک فرایند Node می‌تواند ده‌هزار اتصال همزمان نگه دارد چون به‌ازای هر اتصال نخ ندارد؛ فقط چند کیلوبایت state دارد. این معماری برای I/O-bound عالی است — ولی همین یک نخ، نقطه‌ضعفش هم هست: یک حلقه CPU-bound، همه را گروگان می‌گیرد." },
    { k: "p", t: "Event Loop خودش فاز دارد و دانستن‌شان دیباگ را ساده می‌کند: تایمرها → callbackهای I/O → عملیات‌های سیستمی → setImmediate → بستن‌ها؛ و بعد از هر فاز، کل صف میکروتسک‌ها خالی می‌شود. دو نتیجه عملی: ۱) setImmediate بعد از یک دور I/O اجرا می‌شود ولی process.nextTick قبل از هر فاز بعدی — پس nextTick زیاد یعنی قفل Event Loop؛ ۲) هر چیزی که بیشتر از حدود ۵ میلی‌ثانیه در نخ اصلی طول بکشد، تأخیرش در تمام اتصال‌های همزمان ضرب می‌شود. ابزار شناسایی؟ ماژول worker_threads برای CPU-bound، و Profiler داخلی Node برای پیدا کردن همان گلوگاه." },
    { k: "code", lang: "js", title: "deepdive-blocking.js", code: "// فاجعه: یک حلقه سنگین، ۵۰۰۰ کاربر آنلاین را فریز می‌کند\napp.get('/report', (req, res) => {\n  const start = Date.now();\n  let sum = 0;\n  while (Date.now() - start < 2000) sum += Math.random();  // ❌\n  res.json({ sum });\n});\n// در این ۲ ثانیه: هیچ درخواست دیگری پاسخ نمی‌گیرد!\n\n// راه‌حل ۱: پردازش سنگین به Worker:\nconst { Worker } = require('worker_threads');\nnew Worker('./heavy.js', { workerData: input })\n  .on('message', (result) => res.json(result));\n\n// راه‌حل ۲: شکستن کار به تکه‌های async:\nasync function chunked(arr, fn, size = 1000) {\n  for (let i = 0; i < arr.length; i += size) {\n    fn(arr.slice(i, i + size));\n    await setImmediate(() => {});   // نفس‌کشیدن Event Loop!\n  }\n}\n\n// قاعده طلایی: هر عملیات I/O حتماً async\nawait fs.promises.readFile(path);   // ✓ نخ آزاد می‌ماند\n// fs.readFileSync(path);           ❌ جز در startup، هرگز" },
    { k: "table", head: ["نوع بار کاری", "مناسب Node؟", "راه‌حل"], rows: [["API با I/O زیاد (DB، شبکه)", "عالی", "همان نخ اصلی"], ["پردازش تصویر/ویدیو", "بد", "worker_threads یا صف جدا"], ["محاسبات ریاضی سنگین", "بد", "Worker یا سرویس مجزا"], ["رابط بلادرنگ (WebSocket)", "عالی", "اتصال‌های سبک و زیاد"], ["SSR صفحات پربازدید", "نسبتاً", "کش + Streaming"]] },
    { k: "tip", title: "نکته منتور", t: "برای دیدن Event Loop با چشم خودت، متغیر محیطی NODE_DEBUG=timers را بگذار یا در کد، ترکیب setTimeout/setImmediate/process.nextTick را با console.log چاپ کن و ترتیب را پیش‌بینی کن. ده دقیقه بازی با این ترتیب‌ها، درکی می‌سازد که سوال‌های مصاحبه بک‌اند Node را برایت آب می‌کند." },
  ],

  "c10-l2": [
    { k: "h", t: "عمیق‌تر: REST یعنی قرارداد — کدهای وضعیت و طراحی منابع" },
    { k: "p", t: "REST را نه به‌عنوان «JSON فرستادن»، که به‌عنوان طراحی «منابع و فعل‌های استاندارد» ببین: /orders یک منبع است؛ GET می‌خواندش، POST به آن می‌افزاید، PUT/PATCH تغییرش می‌دهد، DELETE حذفش می‌کند — و URL برای فعل ساختن نیست (/getOrders ضدالگوست). کدهای وضعیت هم زبان مشترک قراردادند: 201 یعنی «ساخته شد» و باید Location بدهد، 204 یعنی «موفق ولی بدنه‌ای نیست»، 400 یعنی «اشتباه از توست» با جزئیات اعتبارسنجی، 401 یعنی «کیستی‌ات معلوم نیست»، 403 یعنی «می‌شناسمت ولی اجازه نداری» — و 500 یعنی «اشتباه از من است». کلاینتی که این تفاوت‌ها را بداند، می‌تواند بدون خواندن مستندات تو، نیمی از API را حدس بزند." },
    { k: "p", t: "سه تصمیم طراحی که کیفیت API را تعیین می‌کنند: اول، نسخه‌بندی — از روز اول /api/v1 بگذار تا فردا بتوانی تغییر شکست‌ساز بدهی بدون نابودکردن کلاینت‌های قدیمی. دوم، صفحه‌بندی برای هر لیست — پاسخ بدون limit می‌تواند فردا با رشد داده، سرور را بکشد؛ cursor-based برای داده زنده، offset برای گزارش‌ها. سوم، شکل خطای استاندارد و ماشین‌خوانا — یک ساختار واحد {error: {code, message, details}} برای همه خطاها، تا کلاینت بتواند برنامه‌نویسی‌اش کند، نه پارس‌کردن متن آزاد." },
    { k: "code", lang: "js", title: "deepdive-rest.js", code: "// طراحی منابع — نه فعل‌ها:\napp.get('/api/v1/orders');            // لیست + صفحه‌بندی\napp.post('/api/v1/orders');           // 201 + Location header\napp.get('/api/v1/orders/:id');        // 200 یا 404\napp.patch('/api/v1/orders/:id');      // تغییر جزئی\napp.delete('/api/v1/orders/:id');     // 204\n\n// پاسخ لیست مهندسی‌شده:\napp.get('/api/v1/orders', async (req, res) => {\n  const { cursor, limit = 20 } = req.query;\n  const { rows, nextCursor } = await orders.list({\n    cursor,\n    limit: Math.min(limit, 100),       // سقف اجباری!\n  });\n  res.json({\n    data: rows,\n    paging: { next_cursor: nextCursor },\n  });\n});\n\n// خطای استاندارد و قابل‌برنامه‌نویسی:\nres.status(422).json({\n  error: {\n    code: 'VALIDATION_FAILED',\n    message: 'ورودی نامعتبر است',\n    details: [{ field: 'email', issue: 'فرمت ایمیل اشتباه است' }],\n  },\n});" },
    { k: "table", head: ["کد", "معنی", "کی برگردانیم"], rows: [["200 / 201 / 204", "موفق — خواندن / ساختن / بدون بدنه", "مسیرهای شاد"], ["400 / 422", "ورودی بد / اعتبارسنجی شکست", "همراه با جزئیات fieldها"], ["401 در برابر 403", "ناشناس / بی‌اجازه", "ترتیب چک: اول authn بعد authz"], ["404", "منبع نیست (یا نباید وجودش لو برود)", "برای منابع محرمانه گاهی 403"], ["409", "تعارض — مثل ثبت تکراری", "با پیام قابل‌اقدام"], ["429", "نرخ درخواست رد شده", "همراه با Retry-After"]] },
    { k: "warn", title: "ضدالگوی ۲۰۰ِ دروغین", t: "برگرداندن 200 با {success: false} برای همه خطاها — رایج‌ترین اشتباه APIهای تازه‌ساز. این کار مانیتورینگ HTTP را کور می‌کند، کش و retry را بی‌معنی می‌کند و هر کلاینتی را مجبور به پارس بدنه می‌کند. کدهای وضعیت رایگان و استانداردند؛ استفاده‌نکردن‌شان یعنی اختراع دوباره چرخ — مربع." },
  ],

  "c10-l3": [
    { k: "h", t: "عمیق‌تر: لایه‌بندی — چرا کنترل‌کننده نباید SQL بداند" },
    { k: "p", t: "سروری که همه‌چیزش در route handler است — اعتبارسنجی، SQL، منطق کسب‌وکار و فرمت پاسخ در یک تابع — شاید اول سریع ساخته شود، ولی به‌مرور تبدیل می‌شود به کدی که هیچ‌کس جرئت تغییرش را ندارد: تست هر endpoint نیاز به بالاآوردن دیتابیس دارد، عوض‌کردن ORM یعنی جست‌وجوی کل پروژه، و منطق کسب‌وکار بین ده route کپی شده. لایه‌بندی این گره را باز می‌کند: کنترل‌کننده فقط HTTP را می‌فهمد (پارس ورودی، کد وضعیت)، سرویس فقط قوانین کسب‌وکار را (بدون هیچ اشاره‌ای به Express)، و Repository فقط دیتابیس را. قانون جهت: وابستگی فقط به سمت داخل — سرویس نمی‌داند داده از Postgres می‌آید یا Mongo." },
    { k: "p", t: "دو جز زیرساختی که جدی‌نگرفتن‌شان حادثه واقعی در تولید می‌سازد: Connection Pool و تراکنش. هر query نباید اتصال تازه بسازد (دست‌دادن TCP + auth حدود ۵۰ms است!) — Pool اتصال‌ها را نگه می‌دارد و امانت می‌دهد؛ اندازه‌اش را هم کوچک نگه دار (حدود دو برابر هسته‌ها) چون دیتابیس با هزار اتصالِ باز بیشتر کند می‌شود نه سریع‌تر. تراکنش هم مرز اتمیته است: کسر موجودی و ثبت سفارش یا هر دو انجام شوند یا هیچ‌کدام — BEGIN/COMMIT/ROLLBACK را یاد بگیر و هر جایی که چند نوشتنِ وابسته داری، تراکنش بگذار. باگ‌های «پول کم شد ولی سفارش ثبت نشد» دقیقاً از غیبت همین دو کلمه می‌آیند." },
    { k: "code", lang: "js", title: "deepdive-layers.js", code: "// Repository — فقط دیتابیس می‌فهمد\nclass OrderRepository {\n  constructor(pool) { this.pool = pool; }   // Pool یک‌بار، همه‌جا\n\n  async findByCustomer(customerId, { cursor, limit }) {\n    const { rows } = await this.pool.query(\n      `SELECT * FROM orders\n       WHERE customer_id = $1 AND id < COALESCE($2, id)\n       ORDER BY id DESC LIMIT $3`,\n      [customerId, cursor, limit]);\n    return rows;\n  }\n}\n\n// Service — فقط قانون کسب‌وکار؛ Express و SQL نمی‌بیند\nclass OrderService {\n  constructor(repo) { this.repo = repo; }\n\n  async cancel(orderId, userId) {\n    const order = await this.repo.findById(orderId);\n    if (order.customerId !== userId) throw new Forbidden();\n    if (order.status !== 'pending') throw new Conflict('فقط سفارش در انتظار لغو می‌شود');\n    return this.repo.updateStatus(orderId, 'cancelled');\n  }\n}\n\n// Controller — فقط HTTP\nrouter.patch('/:id/cancel', wrap(async (req, res) => {\n  await service.cancel(req.params.id, req.user.id);\n  res.status(204).end();\n}));\n// تست service با یک repo ساختگی، بدون دیتابیس، در میلی‌ثانیه!" },
    { k: "p", t: "یک معیار طلایی برای اینکه لایه‌بندی‌ات واقعی است یا نمایشی: آیا می‌توانی service را بدون Express تست کنی؟ آیا می‌توانی بدون تغییر service، دیتابیس را عوض کنی؟ اگر جواب هر دو بله است، معماری‌ات کار می‌کند. و اگر پروژه کوچک است و این لایه‌ها اغراق به‌نظر می‌رسند — حداقل مرز Repository را نگه دار؛ همان یک مرز، روزی که از SQLite به Postgres مهاجرت می‌کنی (و خواهی کرد)، تفاوت بین یک بعدازظهر و یک ماه است." },
    { k: "tip", title: "نکته منتور", t: "برای شروع هر API جدید، اول سه فایل خالی بساز: orders.routes.js، orders.service.js، orders.repository.js — حتی اگر خط اولشان خالی باشد. ساختار قبل از کد، عادت قبل از فشار؛ وقتی deadline نزدیک می‌شود دیگر فرصت فکرکردن به معماری نیست، ساختار از قبل آماده باید باشد." },
  ],
};
